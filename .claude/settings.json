{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n \"$ROOT\" ]; then cd \"$ROOT\" && git add -A 2>/dev/null; if ! git diff --cached --quiet 2>/dev/null; then git commit -m \"auto: session update $(date '+%Y-%m-%d %H:%M')\" -q 2>/dev/null && git push -q 2>&1 || true; fi; fi",
            "timeout": 30,
            "async": true
          }
        ]
      }
    ]
  }
}
